# ToyC 编译器课程实践报告

## 基本信息

- 课程名称：编译系统实践
- 项目名称：ToyC Compiler
- 姓名：梁琮豪
- 学号：2024302111173
- 完成日期：2026 年 8 月

## 1. 实践目标

本项目实现一个面向教学语言 ToyC 的编译器。编译器从标准输入读取 ToyC
源程序，将其翻译为 RV32I/M 汇编并写入标准输出。项目使用 C++20、CMake、
Flex 和 Bison 实现，不依赖 LLVM、Clang IR 或其他现成中间表示。

编译器支持带符号 32 位整数、`int`/`void` 函数、全局与局部变量、常量、
词法作用域、函数调用、条件和循环控制、`break`、`continue`、`return`，以及
算术、关系和短路逻辑运算。命令行参数 `-opt` 用于启用常量条件分支消除、
公共子表达式消除、代数化简、无用临时量删除和寄存器分配等优化。

## 2. 总体设计

编译流程分为四个阶段：

1. Flex 词法分析器将字符流转换为 token，并跳过空白、行注释和 C 风格块注释。
2. Bison C++ 语法分析器依据 ToyC 文法构造项目自有的抽象语法树（AST）。
3. 语义降低阶段维护符号和作用域信息，将 AST 转换为三地址形式的自有中间表示（IR）。
4. 后端为函数分配栈帧和寄存器传参位置，并输出 RV32I/M 汇编。

源代码的主要职责如下：

- `src/lexer.l`：词法规则和 token 生成。
- `src/parser.y`：语法规则、优先级和 AST 构造。
- `src/compiler.hpp`：AST 数据结构与编译器接口。
- `src/compiler.cpp`：语义处理、IR 生成、优化和 RV32 汇编输出。
- `src/main.cpp`：命令行入口、解析调用和错误处理。

## 3. 前端实现

### 3.1 词法分析

词法分析器识别关键字、标识符、十进制整数、单字符运算符和多字符运算符。
多字符运算符包括 `&&`、`||`、`<=`、`>=`、`==` 和 `!=`。注释不会进入语法
分析阶段，从而保持语法规则简洁。

### 3.2 语法分析与 AST

Bison 文件使用 C++ skeleton 和 variant 语义值。运算符的优先级从低到高依次为
逻辑或、逻辑与、相等比较、大小比较、加减、乘除取模和一元运算。悬空 `else`
按 Bison 默认移进规则绑定到最近的 `if`。

AST 使用 `Program`、`Function`、`Decl`、`Stmt` 和 `Expr` 表示编译单元、函数、
声明、语句和表达式。子节点通过 `std::unique_ptr` 管理，减少手工释放造成的错误。

## 4. 中间表示与语义处理

编译器没有使用现成 IR，而是在 `compiler.cpp` 中定义并生成三地址指令。表达式
首先被降低为常量、变量或临时值，控制流则被转换为标签和条件/无条件跳转。

作用域通过逐层符号表管理。进入代码块时建立新作用域，离开时恢复外层作用域。
局部变量映射到当前函数栈帧中的槽位，全局变量输出到数据段。`break` 和
`continue` 使用循环标签栈确定跳转目标；在循环外使用时会被判定为错误。

逻辑与和逻辑或通过控制流实现短路求值：左操作数已经确定最终结果时，不生成
右操作数的执行路径。这既符合语言语义，也避免不必要的函数调用或除法等副作用。

## 5. RV32 目标代码生成

目标代码遵循 RV32 整数调用约定的核心规则：

- 返回值放入 `a0`。
- 前八个整数实参使用 `a0` 至 `a7`。
- 第九个及后续实参通过调用者栈空间传递。
- 每个非叶函数保存并恢复 `ra`。
- 栈帧大小按 16 字节对齐。

算术和比较指令使用 RV32I/M 指令集。函数入口建立栈帧，函数出口统一恢复现场并
返回。全局数据和文本分别输出到相应汇编段，使生成结果可以由 RISC-V GNU 工具链
直接汇编和链接。启用优化时，线性扫描寄存器分配器按临时值的存活区间分配
`s1`--`s11`，这些寄存器在序言和尾声保存恢复；超过寄存器数量的值继续使用栈槽，
因此不会改变调用约定或递归行为。

RV32 的 `addi` 以及 `lw`、`sw` 基址寻址只能编码 12 位有符号立即数，范围为
`[-2048, 2047]`。局部变量、临时量或栈参数较多时，栈帧大小和槽位偏移可能超过
该范围。后端因此统一封装栈指针调整和栈内存访问：立即数在范围内时直接使用
`addi` 或 `offset(sp)`；超出范围时，先用 `li t3, offset` 物化完整偏移，再通过
`add t3, sp, t3` 计算地址，最后使用 `lw/sw 0(t3)` 完成访问。大栈帧的建立与回收
同样使用 `li t3, amount` 和 `add sp, sp, t3`，从而覆盖函数序言、尾声、溢出槽位、
调用者额外实参和被调用者栈参数。

一元负号在 IR 中复用二元减法表示为 `0-x`。目标代码生成阶段将该 IR 零值映射为
RISC-V 架构寄存器 `zero`，生成 `sub target, zero, source`，避免把数字 `0` 误写为
寄存器操作数而导致汇编失败。

## 6. 优化设计

常量表达式折叠在普通模式和 `-opt` 模式下都会执行。例如，仅由常量构成的算术、
比较和逻辑表达式会在编译期求值。

启用 `-opt` 后，编译器还会删除条件在编译期已知的无效控制流。例如恒真 `if`
只保留 then 分支，恒假 `while` 不生成循环体入口。这项优化直接作用于自有 IR 的
生成过程，未借助外部优化框架。

此外实现了以下 IR 后端优化算法：

1. **局部公共子表达式消除**：在每个基本块内进行值编号，对 `Load` 和二元运算建立
   表达式键；重复计算改为复用已有临时值。遇到标签、跳转、写内存或函数调用时清除
   可能失效的记录，并对 `+`、`*`、`==`、`!=` 做操作数规范化。
2. **代数恒等式与强度化简**：将 `x+0`、`x-0`、`x*1` 和 `x/1` 转换为简单复制，
   将 `x*0` 转换为常量零，从而避免执行没有必要的算术指令，并为后续无用值删除
   创造机会。
3. **迭代无用临时量删除**：统计虚拟临时量的使用次数，删除结果无人使用且没有
   副作用的 `Mov`、`Bin` 和 `Load` 指令。删除一条指令可能使其输入也失去用途，
   因此算法重复扫描直到 IR 不再变化。函数调用、内存写入和控制流指令始终保留，
   避免破坏可观察行为。
4. **冗余跳转清理**：如果无条件跳转的目标就是紧随其后的标签，则删除该跳转，
   让控制流直接顺序落入目标基本块，减少一次运行时跳转。
5. **线性扫描寄存器分配**：收集临时值首次定义到最后使用的区间，按开始位置扫描并
   复用已结束区间的 `s1`--`s11`。没有可用寄存器时保留栈分配，保证算法在高寄存器
   压力下仍然正确。

优化管线先执行公共子表达式消除，再进行代数化简、迭代无用值删除和跳转清理，
最后在目标代码生成前完成线性扫描寄存器分配。这样的顺序可以让前一阶段产生的
冗余临时量继续被后一阶段消除，并减少栈槽、访存和算术/跳转指令数量。

## 7. 构建与测试

项目在 Ubuntu WSL 环境中使用 Flex 2.6.4、Bison 3.8.2、GCC 11.4 和 CMake
完成构建。构建命令如下：

```sh
cmake -S . -B build
cmake --build build -j
```

端到端测试脚本 `tests/run_wsl.sh` 会重新构建编译器，分别以普通模式和 `-opt`
模式编译递归及控制流用例，再使用 GNU RISC-V 工具链按 `rv32im/ilp32` 生成无
libc 静态程序，最后通过 `qemu-riscv32` 执行。

实测结果如下：

```text
PASS recursion: exit=120
PASS recursion_opt: exit=120
PASS control_flow: exit=1
PASS control_flow_opt: exit=1
PASS optimizations: exit=1
PASS optimizations_opt: exit=1
PASS register_pressure: exit=78
PASS register_pressure_opt: exit=78
PASS codegen_limits_normal: exit=253
PASS codegen_limits_opt: exit=253
PASS large_stack_normal: exit=31
PASS large_stack_opt: exit=31
PASS many_arguments_normal: exit=87
PASS many_arguments_opt: exit=87
PASS optimization shape: add 9->5, mul 3->2
```

递归测试验证了函数调用、参数、返回值和递归栈帧；控制流测试验证了局部变量修改、
循环、分支、`break` 和 `continue`。此外还单独编译了九参数函数调用，用于检查
寄存器参数与栈参数的衔接。优化专项测试在普通模式和 `-opt` 模式下均返回 1，
说明优化没有改变程序结果；生成汇编中的加法由 9 条减少到 5 条，乘法由 3 条
减少到 2 条，验证了公共子表达式消除及后续清理对目标代码规模和运行指令数的改善。
另外使用包含 `(x+0)*1` 和未使用 `x*0` 的专项输入检查代数化简与无用值删除：
普通模式生成 1 条加法和 2 条乘法，`-opt` 模式下两类指令均减少为 0 条。

针对线上测试暴露的汇编边界问题，测试脚本还会动态生成三组源码：变量一元负号、
800 个局部变量形成的大栈帧，以及包含 600 个形参和实参的函数调用。三组测试分别
以 253、31 和 87 作为预期退出码，并在普通模式和 `-opt` 模式下完成 ToyC 编译、
RV32 汇编链接和 QEMU 执行。这些用例验证了 `zero` 寄存器选择、大于 2 KiB 的栈帧、
大偏移保存恢复以及超大栈参数区的正确性。

## 8. 实践总结

本实践完成了从源语言文本到可执行 RISC-V 目标代码的完整路径。实现过程中，
最关键的工作是让词法/语法结构、作用域规则、自有 IR 和调用约定保持一致。
短路逻辑不能简单翻译为普通二元运算，超过八个参数的调用也要求调用者和被调用者
对栈布局采用完全一致的约定；目标指令立即数的编码范围也必须由后端显式处理，不能
假设任意栈帧大小都能直接写入一条指令。通过 QEMU 端到端执行生成程序，可以同时
验证前端、控制流降低、栈帧和目标指令，而不只停留在汇编文本检查层面。

当前实现满足课程指定的主要语言特性和构建方式。后续可以继续增加非法程序诊断、
更多边界测试，以及基于控制流图的全局常量传播、循环不变量外提等跨基本块优化。
